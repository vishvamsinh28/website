coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

comment:
  layout: "header, diff, files, footer"
  behavior: default
  require_changes: false
  after_n_builds: 1

  show:
    files: true
    diff: true
    unchanged: true
    layout: "files"
