coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 1%

comment:
  layout: "reach, diff"
  behavior: default
  require_changes: yes

parsers:
  javascript:
    enable_partials: yes