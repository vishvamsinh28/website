coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
        base: auto
    patch:
      default:
        target: auto
        threshold: 0%
        base: auto

comment:
  layout: "reach,diff,flags,tree,files,footer"
  behavior: default
  require_changes: no

ignore:
  - "test/**/*"
  - "**/*.test.js"